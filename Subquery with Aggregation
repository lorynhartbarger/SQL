/*For this project, I wrote a subquery to find the business unit name, customer name, 
and revenue for business units with average revenue greater than 100,000 */

SELECT 
CUSTOMER_ID,
SUM(ORDER_TOTAL) AS TOTAL_ORDER_AMNT
FROM ORDER_INFO
GROUP BY CUSTOMER

                
